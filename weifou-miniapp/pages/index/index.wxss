/* 首页 = 全屏沉浸界面：立绘铺底 + 糖果亮场白雾 + 所有操作白玻璃半透明浮层（游戏式单界面） */
.page {
  position: relative;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  box-sizing: border-box;
  padding: calc(env(safe-area-inset-top) + 72rpx) 48rpx calc(env(safe-area-inset-bottom) + 44rpx);
  overflow: hidden;
  /* 全背景基底：糖果亮渐变（立绘叠其上；无立绘的老用户态也有沉浸底） */
  background: var(--grad-sky);
}

/* 全屏立绘（固定，不随内容滚） */
.bg-lihe { position: fixed; inset: 0; width: 100%; height: 100%; z-index: 0; }
/* 亮雾：上极轻（标题可读）+ 下白托底重（按钮/气泡可读） */
.bg-scrim {
  position: fixed; inset: 0; z-index: 1;
  background: linear-gradient(to bottom,
    rgba(255,255,255,0.20) 0%, rgba(255,255,255,0.04) 26%,
    rgba(255,255,255,0.35) 62%, rgba(255,255,255,0.90) 100%);
}

/* 内容浮在最上层 */
.hero, .grow, .float, .actions, .loading { position: relative; z-index: 2; }

.brand { font-size: 26rpx; color: var(--ink-2); letter-spacing: 8rpx; margin-bottom: 10rpx; }
.tagline { font-size: 30rpx; font-weight: 600; color: var(--ink); letter-spacing: 1rpx; margin-bottom: 22rpx; }
.headline {
  font-size: 56rpx; font-weight: 800; color: var(--ink); line-height: 1.4;
}

.grow { flex: 1; min-height: 40rpx; }

/* 示例助理：名牌 + 招呼气泡（白玻璃） */
.float { margin-bottom: 30rpx; }
.float-id { display: flex; align-items: center; gap: 10rpx; margin-bottom: 14rpx; }
.float-name { font-size: 28rpx; font-weight: 600; color: var(--ink); }
.dot { width: 12rpx; height: 12rpx; border-radius: 50%; background: #34d399; animation: presencePulse 2s ease-in-out infinite; }
@keyframes presencePulse { 0%,100% { box-shadow: 0 0 0 0 rgba(52,211,153,0.5); } 50% { box-shadow: 0 0 0 8rpx rgba(52,211,153,0); } }
.float-on { font-size: 22rpx; color: var(--ink-2); }
.float-bubble {
  background: var(--glass-dark);
  backdrop-filter: blur(24rpx); -webkit-backdrop-filter: blur(24rpx);
  border: 1rpx solid var(--border);
  color: var(--ink); font-size: 27rpx; line-height: 1.6;
  padding: 22rpx 26rpx; border-radius: 24rpx; border-bottom-left-radius: 8rpx;
}
.float-note { margin-top: 14rpx; font-size: 22rpx; color: var(--sub); }
.caret {
  display: inline-block; width: 3rpx; height: 28rpx; background: var(--accent);
  margin-left: 4rpx; vertical-align: -4rpx; animation: caretBlink 1s step-end infinite;
}
@keyframes caretBlink { 0%,100% { opacity: 1; } 50% { opacity: 0; } }

/* 操作区：白玻璃 3D 糖果按钮 */
.actions { display: flex; flex-direction: column; gap: 20rpx; }
.glass-btn {
  background: var(--glass-light);
  backdrop-filter: blur(24rpx); -webkit-backdrop-filter: blur(24rpx);
  border: 1rpx solid var(--border);
  color: var(--ink);
  border-radius: 100rpx;
  padding: 26rpx 0; text-align: center;
  font-size: 30rpx; font-weight: 500;
  box-shadow: 0 6rpx 0 var(--border);
  transition: transform 0.12s var(--ease), box-shadow 0.12s var(--ease);
}
.glass-btn:active { transform: translateY(6rpx); box-shadow: none; }
/* 主操作：实心绿糖 CTA */
.glass-btn.primary {
  background: var(--accent);
  border-color: var(--accent);
  color: var(--on-accent);
  font-weight: 800;
  box-shadow: 0 8rpx 0 var(--accent-strong);
}

.loading { color: var(--ink-2); text-align: center; margin-top: 60rpx; }

/* 收件箱待办角标：红色小徽章浮在按钮右上角（主人召回钩子） */
.inbox-btn { position: relative; }
.todo-badge {
  position: absolute;
  top: -12rpx;
  right: 28rpx;
  min-width: 38rpx;
  height: 38rpx;
  padding: 0 12rpx;
  box-sizing: border-box;
  border-radius: 100rpx;
  background: var(--danger);
  color: var(--on-accent);
  font-size: 24rpx;
  font-weight: 600;
  line-height: 38rpx;
  text-align: center;
  box-shadow: 0 4rpx 12rpx rgba(255, 75, 75, 0.4); /* 红点外发光：alpha 烘死，随皮不换 */
}
