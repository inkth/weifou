/* 组件默认 styleIsolation:isolated，app.wxss 的 .ic 不会下传，需自行 import 图标真源 */
@import "../styles/icons.wxss";

/* 毛玻璃悬浮底栏：半透明 + 背景模糊，页面内容从栏下透出 = Apple 式「悬浮毛玻璃」。
   活跃态用 M3 式「胶囊指示器」（accent-soft 底 + 强调色图标）。
   仅在 tabBar 页面渲染，沉浸页不出现。 */
.tabbar {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 1000;
  display: flex;
  padding: 12rpx 0 calc(env(safe-area-inset-bottom) + 8rpx);
  background: rgba(255, 255, 255, 0.78);
  backdrop-filter: blur(40rpx) saturate(180%);
  -webkit-backdrop-filter: blur(40rpx) saturate(180%);
  border-top: 1rpx solid rgba(31, 35, 48, 0.06);
  box-shadow: 0 -2rpx 24rpx rgba(31, 35, 48, 0.05);
}
.tab {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4rpx;
  padding: 4rpx 0;
  color: #8a8f9c; /* = currentColor → 驱动图标颜色 */
}
/* 活跃指示胶囊：图标外的圆角底，活跃时升起暖底 */
.tab-pill {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 104rpx;
  height: 56rpx;
  border-radius: 999rpx;
  transition: background 0.24s cubic-bezier(0.16, 1, 0.3, 1);
}
.tab-ic {
  font-size: 42rpx;
  transition: transform 0.24s cubic-bezier(0.16, 1, 0.3, 1);
}
.tab-label {
  font-size: 22rpx;
  color: #8a8f9c;
  letter-spacing: 0.5rpx;
  transition: color 0.18s ease;
}
.tab.active {
  color: #18b690;
}
.tab.active .tab-pill {
  background: #e2f5ef;
}
.tab.active .tab-ic {
  transform: scale(1.04);
}
.tab.active .tab-label {
  color: #18b690;
  font-weight: 600;
}
.tab:active .tab-pill {
  transform: scale(0.92);
}
